import java.io.*;
public class Main {

    public static void main(String[] args) throws Exception {
        /**
         * Compute Hash works
        String input = args[0];
        SHA256 hasher = new SHA256();
        hasher.computeHash(input);
        */
        /** 
        float[] ELOs = { 1500, 1700 };
        boolean[] hasWon = {false, true};
        ELOCalculator elocalc = new ELOCalculator(ELOs, hasWon);
        int[] newELOs = elocalc.calculateELOs();
        for (int a = 0; a < 2; a++) {
            System.out.println(newELOs[a]);
        } ELO works
        */ 
    }
}